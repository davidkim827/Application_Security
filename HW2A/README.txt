How to run:

0. should install flask as a python module from pypi (run python3 -m pip install flask on command line)
1. set environment variable, FLASK_APP = ..abs/path/of/webapp.py
2. run flask, and take note of the port number that the webapp is running on
3. go to browser and input localhost:port#_given_before
4. enjoy the vulnerabilities!